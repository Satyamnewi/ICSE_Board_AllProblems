class maths
{
    public static void main(String args[])
    {
        double a=5.121256,b=4.23256;
        double r=0;
        r=Math.sqrt(Math.ceil(a)+Math.floor(b));
        System.out.println(r);
    }
}